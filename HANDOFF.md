# 인계 문서 — FC Barcelona 대시보드

작성 시점 기준 상태와, 이어서 작업할 때 알아야 할 것들.

## 한 줄 요약

Streamlit 멀티페이지 17개. football-data.co.uk(라리가 경기), FBref(선수·전 대회),
StatsBomb·Understat(이벤트), Transfermarkt(사진·감독) 다섯 소스를 직접 수집해
집계한다. 모든 수치는 원본에서 계산하며 어디까지 덮는지 페이지에 명시한다.

## 실행

```bash
pip install -r requirements.txt       # 대시보드 실행용 (Cloud도 이걸 본다)
pip install -r requirements-dev.txt   # 수집·학습까지 돌릴 때
streamlit run app.py                  # 기본 8501, 개발 중에는 8531을 썼다
```

`.claude/launch.json`(워크스페이스 루트)에 `barcelona` 항목이 있고 포트는 8531이다.
8520은 EPL 대시보드가 이미 쓰고 있어 피했다.

## 구조

```
app.py                     st.navigation 진입점. 페이지 등록만 한다
_lib.py                    공통 CSS·팔레트·데이터 로더·피치 도형·사진 매칭
views/                     페이지 17개
  club.py                  홈 — 34시즌 요약 (완료 33 + 진행 중 1)
  eras.py                  역사·시대 분석 (감독 기준 8시대)
  legends.py               레전드 TOP 10 (카드 선택 → 상세)
  managers.py              역대 감독 (카드 선택 → 상세 + 한 줄 평)
  clasico.py               엘클라시코 (역사 서술·갤러리·전적)
  competitions.py          대회별 성적
  players.py               선수 아카이브 (대회별 + 사진)
  masia.py                 라 마시아 (B팀 출신의 시즌별 1군 출전 비중)
  advanced.py              선수 고급 기록 (이벤트 기록 경기당·레이더)
  tikitaka.py              티키타카 지수 (패스 3축 합성 + 점유율)
  network.py               연계 네트워크 (어시스트 조합 + 시즌별 구조)
  msn.py                   MSN 삼각편대 (2014/15~2016/17)
  shots.py                 xG·슈팅 맵
  passes.py                패스 맵
  seasons.py               시즌 기록 검색
  model.py                 AI 모델
  coverage.py              데이터 제공 범위
```

## 수집 스크립트 (실행 순서)

| 스크립트 | 하는 일 | 소요 |
|----------|---------|------|
| `build_data.py` | raw CSV → 시즌 지표·순위표·클라시코·전체 경기 | 즉시 |
| `crawl_fbref.py` | FBref 라리가 선수 스탯 8종 × 33시즌 (264건) | ~45분 |
| `build_players.py` | 위 8종을 바르사 선수-시즌 한 장으로 병합 | 즉시 |
| `crawl_allcomps.py` | FBref 클럽 전 대회 경기 + **대회별 선수 스탯** | ~7분 |
| `fetch_statsbomb.py` | StatsBomb 이벤트 531경기 | ~14분 |
| `fetch_understat.py` | Understat 슛 2014/15~2025/26 | ~15분 |
| `crawl_portraits.py` | TM 선수 증명사진 323장 | ~22분 |
| `crawl_managers.py` | TM 감독 이력 + 사진 | ~2분 |
| `build_managers.py` | 재임 기간 × 라리가 경기로 감독 성적 집계 | 즉시 |
| `crawl_masia.py` | 바르사 B팀 명단 (라 마시아 판별용) | ~4분 |
| `fetch_images.py` | 커먼즈 이미지 (레전드·클라시코·홈·시대) | 오래 걸림 |
| `apply_inbox.py` | `assets/_inbox/`에 넣은 사진을 자산으로 반영 | 즉시 |
| `train_model.py` | 경기 결과 예측 모델 학습 | 즉시 |

## 데이터 현황

| 산출물 | 규모 |
|--------|------|
| `data/processed/club_season.parquet` | 34시즌 (완료 33 + 진행 중 2026/27) |
| `data/processed/club_matches.parquet` | 1,265경기 |
| `data/processed/all_matches.parquet` | 라리가 전 구단 12,734경기 |
| `data/processed/players.parquet` | 886행 · 라리가 상세 |
| `data/fbref_allcomps_players/` | 3,443행 · 367명 · 대회별 |
| `data/fbref_allcomps/` | 1,716경기 (챔스 325 포함) |
| `data/statsbomb/` | 531경기 · 슛 13,140 · 패스 367,738 |
| `data/understat/shots.parquet` | 슛 11,508 |
| `data/processed/masia.json` | B팀 출신 339명 |
| `assets/portraits/` + `_thumb/` | 323장 |
| `assets/managers/` | 25장 |

라 마시아 페이지는 역사 연표·대표 선수 5명·AI 계승 이미지를 포함한다. 계승 이미지는
`assets/masia_lineage.jpg`이며 AI 생성임을 캡션에 밝힌다. 이미지 위 얼굴 오버레이는
시각적으로 어울리지 않아 제거했고, 다시 넣지 않는 편이 낫다.

## 반드시 알아야 할 함정

### 1. Streamlit 캐시 키에 파일 수정 시각을 넣어야 한다

`@st.cache_data`를 인자 없는 로더에 걸면, 수집 스크립트를 다시 돌려 파일이
바뀌어도 **옛 내용이 계속 나온다**. 실제로 세 번 당했다(스토이치코프 사진이
안 나옴, 감독 사진이 이니셜로 남음, 모델 `KeyError: 'auc_win'`).

`_lib.py`의 `load_parquet` / `load_json` / `b64` / `load_dir`를 쓸 것.
페이지에서 `@st.cache_data`로 파일을 직접 읽지 말 것.

### 2. 라리가 순위는 상대전적을 먼저 본다

승점 동률이면 **동률 팀 간 상대전적 → 상대전적 골득실 → 전체 골득실** 순이다.
골득실을 바로 적용하면 2006/07이 바르사 우승으로 잘못 나온다(실제로는 레알이
상대전적 우위). `build_data.py`의 `_head_to_head()`가 이걸 처리한다.
**바르사 리그 우승은 이 규칙으로 16회다.**

### 3. FBref가 라리가 세부 지표를 빈 값으로 준다

패스 성공률·태클·터치·드리블 등 28개 열이 응답 자체에서 비어 있다(2024/25도,
2017/18도 동일). 재시도로 해결되지 않는다. `build_players.py`가 전 시즌 결측
열을 자동 제외한다. 그 지표들은 StatsBomb 이벤트로 대체했다.

### 4. FBref 표 맨 아래 합계 행

`Squad Total` / `Opponent Total`이 선수 행에 섞인다. `crawl_allcomps.py`의
`TOTAL_ROWS`가 걸러낸다. 다른 FBref 표를 새로 파싱하면 같은 처리가 필요하다.

### 5. FBref standard 스탯의 URL 경로는 `stats`

`/comps/12/{season}/standard/...`가 아니라 `/comps/12/{season}/stats/...`다.
골키퍼 테이블 id는 단수형 `stats_keeper`. `crawl_fbref.py`의 `STAT_TYPES` 참고.

### 6. Understat은 requests에 데이터를 안 준다

평범한 HTTP 요청에는 `shotsData`가 빠진 HTML이 온다. 브라우저로 열어야
`window.shotsData`가 채워진다. `fetch_understat.py`가 undetected-chromedriver로
JS 변수를 직접 읽는다. 오래 돌리면 렌더러가 타임아웃을 내므로 40건마다
드라이버를 재시작한다.

### 7. 커먼즈 이미지 다운로드는 429가 잦다

`upload.wikimedia.org`가 IP 단위로 레이트리밋을 오래 건다. `fetch_images.py`는
요청 간 25초 + 429 시 75/180/360초 백오프를 쓴다. 그래도 실패하면 후보
파일명이 틀린 경우가 많으니 커먼즈 검색으로 실제 이름을 확인할 것.

### 8. 사진은 `assets/_inbox/`로 넣는다

사용자가 직접 고른 사진을 쓸 때는 `assets/_inbox/{key}.jpg`에 놓고
`python apply_inbox.py`를 실행한다. 키는 `apply_inbox.py`의 `TARGETS` 참고
(messi, cruyff, puyol, figo, sunyol …). 여러 페이지가 쓰는 인물은 양쪽에
복사되고 출처가 "사용자 제공"으로 기록된다.

### 9. 양방향 곡선은 법선을 고정된 순서로 구할 것

두 노드 사이에 방향이 둘일 때(A→B, B→A) 곡선을 반대쪽으로 휘려면, 법선을
**항상 같은 순서의 두 점**에서 구하고 부호만 방향에 따라 줘야 한다.
`a→b` 좌표로 그때그때 구하면 방향이 뒤집힐 때 법선도 뒤집혀 부호와 상쇄되고,
두 곡선이 같은 자리에 겹쳐 그려진다. MSN 삼각형에서 여섯 방향 중 셋만 보이던
원인이 이거였다. `views/msn.py`의 `lo, hi = sorted((a, b), key=TRIO.index)`
부분 참고.

### 10. FBref 대회별 표에 팀 합계 행이 섞인다

`Squad Total` / `Opponent Total`이 선수 행처럼 들어와 "최다 득점 578골
Squad Total" 같은 결과가 나온다. `crawl_allcomps.py`의 `TOTAL_ROWS`가
걸러낸다. 다른 FBref 표를 새로 파싱하면 같은 처리를 넣어야 한다.

### 11. 이름 매칭은 정규화 + 성 보조로

소스마다 표기가 다르다 — `Ferrán`/`Ferran`, `Rakitic`/`Rakitić`,
`Anssumane Fati`/`Ansu Fati`, `Ion`/`Jon Goikoetxea`. `_lib._name_key()`로
발음기호를 벗기고, 그래도 없으면 성으로 한 번 더 찾되 **성이 겹치면 제외**해
엉뚱한 사람이 붙지 않게 한다. 이 방식으로 포지션 매칭 80% → 97%,
사진 매칭 75% → 79%가 됐다. `portrait_map()` / `position_map()` 참고.

## 알려진 한계 (페이지에도 명시돼 있음)

- **2004/05 원본 보정 완료** — trailing odds 열 때문에 누락된 11경기를 복구해 38경기로 맞췄다.
  그 시즌 엘클라시코 두 경기(3-0, 2-4)도 포함된다.
- **컵대회 미포함(일부 페이지)** — 홈·시대분석·엘클라시코·감독 성적은 라리가만
  집계한다. 클럽 공식 통산과 숫자가 다른 이유. 전 대회 데이터는 별도로 있다.
- **패스 맵은 2020/21까지** — 패스 좌표를 주는 무료 소스가 StatsBomb뿐이고
  라리가 공개 범위가 거기서 끝난다. Understat은 슛만 준다. FBref는 라리가
  이벤트가 없다. FotMob API는 토큰이 필요하다.
- **StatsBomb 출전 시간 미산출** — 저장된 집계 파일은 실제 출전시간을 제공하지 않는다.
  마지막 등장 분을 이용한 임의 분모는 폐기했고, 고급 기록은 이벤트 기록 경기당으로 표시한다.
  교체 출전과 풀타임의 노출량 차이는 보정하지 않는다.
- **감독 명단 보충** — TM 이력에 없는 카를레스 레샥(2001/02), 티토 빌라노바
  (2012/13), Jesús Antonio de la Cruz(2003-02-01)를 `build_managers.py`의 `MISSING`에 넣었다.
- **임시 감독 표시** — 원본에 역할 구분이 없어 `CARETAKERS`에 직접 적었다.
- **선수 사진 매칭 79%** — 1990년대 선수는 TM에 사진이 아예 없다.

## AI 모델에 대해

정확도로는 기준선을 **못 넘는다**. 바르사가 리그에서 70.5% 이기므로 "항상 승"만
찍어도 70.5%가 나오고 모델은 70.0%다. 로그손실도 기준선과 거의 같다.

다만 한 번의 시간순 평가 구간에서 승 확률 AUC 0.629, 상위 25% 실제 승률 83.3% vs
하위 25% 58.3%가 관찰됐다. 일반화는 미검증이다. 정확도를 올리려고 경기 중
기록(슛·코너)을 피처에 넣으면 예측이 아니라 결과 되짚기가 되므로 하지 말 것.

## 새로 만든 것 (이번 차례)

- **티키타카 지수** (`views/tikitaka.py`) — 경기당 패스·성공률·짧은 패스 비율
  세 축을 각각 0~100으로 늘려 평균 낸 합성 지표. 공식 지표가 아니라 이
  대시보드가 정의한 값임을 페이지에 밝혔다. 정점은 2012/13(95), 2020/21(92),
  2010/11(87). StatsBomb이 2020/21에서 끊겨 그 뒤는 FBref 점유율로 잇는다.
- **연계 네트워크** (`views/network.py`) — 어시스트 조합. Understat이 골마다
  득점자와 도움을 함께 줘서 이걸 썼다. StatsBomb 패스에도 어시스트 표시는
  있지만 **받은 사람이 없어 조합을 만들 수 없다**. 최다 조합은 수아레스→메시
  34골, 반대 방향 32골.
- **MSN 삼각편대** (`views/msn.py`) — 세 사람의 연혁·트로피·통산·공격 루트.
  3시즌 357골로 팀 득점의 71%(2015/16은 76%). 셋 사이에서만 85골이 나왔다.
  통산은 FBref 전 대회 합산, 공격 루트와 삼각형은 Understat 라리가 골이다.
- **시즌별 연계 구조** (`views/network.py` 안) — 도움·득점 관여가 얼마나 고르게
  나뉘었는지를 허핀달 지수로 재 0~100 분산도로 표시. **직관과 반대로 MSN
  시대(2014~16)가 가장 집중돼 있고 메시가 떠난 2021/22가 가장 분산된다.**
  분산도가 높다고 좋은 팀은 아니라는 점을 페이지에 명시했다.
- **감독 전술 성향** (`views/managers.py` 안) — 경기당 슛·유효슛률·코너·파울·
  허용 슛·경고. 지표를 골라 감독끼리 세운다. 허용 슛처럼 낮을수록 좋은 지표는
  정렬과 색을 뒤집었다.

## 이번 세션에서 끝낸 것

### 홈 — 데이터 센터 레이아웃 (`views/club.py`)
첫 화면이 클럽 소개로 시작하던 것을 최신 시즌 실측치로 바꿨다.
히어로 카드(최종전 스코어) + 순위·전적·승률·최근 10경기 폼, 그 아래 3열로
최근 6경기 결과 / 득점 리더 / 도움 리더(선수 사진 포함).

**라리가 전체 순위표가 있다.** 한동안 "원본에 바르사 경기만 있어 못 만든다"고
잘못 알려져 있었으나, `data/raw/SP1_*.csv` 33개 파일에 각 시즌 20개 팀(1995/96·
1996/97 은 22팀) 전 경기가 들어 있다. 게다가 `build_data.py` 의 `league_table()`
이 예전부터 이미 그걸 계산하고 있었다.

`data/processed/standings.parquet` — 34시즌 684행. 라리가 규정대로
승점 → 동률 팀 간 상대전적 → 골득실 → 다득점 순으로 세운다. 골득실을 바로
적용하면 2006/07 처럼 실제와 뒤집히는 시즌이 생긴다(바르사·레알 76점 동률에서
상대전적으로 레알 우승). 바르사 순위는 시즌별 `club_season.rank` 와 대조한다.

### 삼각편대의 역사 (`views/msn.py`)
MSN 단독 페이지를 1기(MVP) → 2기(MSN) 시간순 한 페이지로 넓혔다.
파일명은 `msn.py` 그대로다(URL `/msn` 유지).

- 1기: 배너·카드 사진·연혁·트로피·시즌별 득점·삼각형·기록 표
- 2기: 기존 구역 유지
- 맨 끝에 1기 vs 2기 비교 (시즌 수가 달라 **시즌당**으로 맞춤)
- `draw_triangle()` 함수로 삼각형 그리기를 통합 (중복 105줄 제거)

**삼각형 겹침의 진짜 원인**은 한 변의 두 곡선 중 한쪽이 삼각형 **안쪽**으로
휘어 그 라벨들이 무게중심에 쌓인 것이었다. 법선을 항상 바깥으로 돌리고
반지름만 다르게(0.45 / 1.65) 줘 동심 아치로 만들면 여섯 개가 자연히 흩어진다.
라벨은 골 수만 남기고 방향은 아래 표로 읽는다.

### StatsBomb 어시스트 연결 (`fetch_statsbomb.py`)
슛의 `shot.key_pass_id` 는 그 슛을 만든 **패스 이벤트의 id** 다. 같은 경기의
Pass 이벤트에서 그 id 를 찾으면 도움 준 선수가 나온다. `assisted_by` 열로
넣었고, 캐시 531건 재파싱으로 네트워크 호출 없이 끝난다.

- 슛 13140건 중 9408건에 도움 기록. 바르사 골 1365 중 931건(68%)
- 시즌 범위 **1973/74~2020/21** — Understat(2014/15~)보다 훨씬 넓다
- 시즌 표기는 `2010/11` 형식이다 (`2010/2011` 아님, 여기서 한 번 헤맸다)
- 이름은 전체 이름이다(`Lionel Andrés Messi Cuccittini`). `SB_MVP` 같은
  대응표로 FBref 짧은 이름과 이어야 한다.

**1기 삼각형 29골** 검증: 메시→비야 8, 메시→페드로 8, 페드로→메시 7,
비야→메시 5, 페드로→비야 1, 비야→페드로 0.

2기가 85골(Understat)인데 1기가 29골인 것은 **원본 차이가 아니다**. 같은
StatsBomb으로 2기를 재도 81골이 나온다(두 원본이 서로를 검증한다). 시즌당
14.5 대 27.0으로 실제 차이다. 비야·페드로 2시즌 57골 대 수아레스·네이마르
3시즌 205골이고, 1기는 셋끼리보다 차비·이니에스타·알베스에게 받았다
(도움자 상위: 메시 34, 알베스 21, 차비 14, 이니에스타 12, 페드로 11).

### 연계 네트워크 — 2005/06까지 확장 (`views/network.py`)
Understat(2014/15~)만 쓰던 것을 StatsBomb(2005/06~2013/14)과 붙였다.
12시즌 → **21시즌**, 연계 골 782 → **1,264건**. 겹치는 시즌은 Understat 을 쓴다.

골 20건 미만 시즌은 뺐다(`MIN_GOALS`). StatsBomb 에는 1973/74(5골)처럼 몇
경기만 든 시즌이 있어 그대로 두면 시즌별 비교가 엉킨다. 그래서 2004/05가
아니라 2005/06부터다.

**이름 맞추기**(`_lib.sb_names`)가 이 작업의 핵심이었다. 그냥 붙이면 메시가
`Lionel Messi` 와 `Lionel Andrés Messi Cuccittini` 두 사람으로 쪼개진다.
성이 겹치고 이름 첫 토큰이 서로의 접두사면 같은 사람으로 본다
(`Daniel Alves da Silva` ↔ `Dani Alves`). de/da/i/y 같은 관사는 비교에서 뺀다.
그걸로 안 되는 19명은 `SB_ALIAS` 에 명시했다(`Xavier Hernández Creus` → `Xavi`).
옛 시즌 선수는 FBref(1993/94~)에 아예 없어 맞출 상대가 없다 — 전체 이름을 둔다.

### 1기 주 공격 루트 (`views/msn.py`)
StatsBomb 으로 상황별·부위별 득점, 마무리 기술, 공을 대준 사람을 넣었다.
**두 원본은 값 이름이 다르다** — Understat `OpenPlay` vs StatsBomb
`Regular Play`. 그래서 표(`SB_PATTERN`/`SB_BODY`/`SB_TECH`)를 따로 뒀다.

2기의 '골 직전에 무슨 일이 있었나'는 Understat 의 `lastAction` 항목이라
StatsBomb 에는 없다. 대신 `technique`(마무리 기술)으로 대체했다.

확인된 값: 1기 셋의 골 122, 왼발 81 / 오른발 35 / 헤더 5.
셋에게 공을 대준 사람 1위는 **알베스 16회**(이니에스타 8, 차비 5).

### 골 직전 상황 (`assist_kind`)
Understat 의 `lastAction` 은 StatsBomb 에 없다. 대신 **골로 이어진 패스 자체의
속성**으로 같은 자리를 채웠다(`fetch_statsbomb.py` 의 `assist_kind`).
세트피스 → 스루패스 → 컷백 → 크로스 → 재개 상황 → 띄운 패스 → 짧은 패스
순으로 판정한다. **순서가 중요하다** — 코너에서 올린 크로스는 '크로스'가
아니라 '세트피스'로 봐야 한다. 도움이 없으면 '직접'(개인 돌파·직접 프리킥).

같은 원본으로 두 조합을 재니 전술 차이가 그대로 드러났다.

| | 1기 | 2기 |
|---|---|---|
| 스루패스 | **27.0%** | 19.1% |
| 크로스 | 6.6% | **13.4%** |
| 직접 | 33.6% | 32.9% |

발 빠른 셋이 뒷공간으로 달리고 거기로 찔러 넣던 1기와, 서로 주고받으며
측면을 쓰던 2기의 차이다.

### 사진


- 삼각편대 1·2기 선수 사진을 모두 **바르사 유니폼**으로 교체(사용자 제공)
- 라 마시아 현 세대 5명도 교체. 커먼즈에는 현역 선수의 자유 라이선스 경기
  사진이 사실상 없다 — 통신사 보도사진이라 올라오지 않는다. 직접 받아야 한다.
- `apply_inbox.py` 에 `mvp` / `msn` / `masia` 저장소와 키 추가.
  키와 파일명이 다른 경우를 위해 `FILENAME` 대응표를 뒀다
  (`mvp_messi` → `mvp/messi.jpg`. `legends/messi` 와 겹치지 않게 키만 나눈 것)

### CSS 함정 — 이게 두 번 발목을 잡았다
Streamlit 기본 스타일이 `img` 에 `object-fit: scale-down` 을 걸어 둔다.
`!important` 없이는 우리 값이 **조용히 무시된다**. `.msn-photo` 와
`.era-photo` 가 이것 때문에 크기가 제각각이었다. 이미지 관련 CSS를 새로
쓸 때는 `!important` 를 붙이고, 반드시 브라우저에서 `getComputedStyle` 로
실제 적용값을 확인할 것.

## 검수 도구

### `tools/audit.py`
대시보드가 쓰는 데이터의 논리적 모순 46개 항목을 훑는다.
모순이 있으면 종료 코드 1. **데이터를 다시 만든 뒤에는 반드시 돌린다.**

```
"/d/workspace/EPL project/.venv/Scripts/python.exe" tools/audit.py
```

세션 임시 폴더에만 두었다가 매번 다시 쓰던 것을 저장소로 옮겼다.

## 서브에이전트 세 종 (`.claude/agents/`)

| 에이전트 | 담당 | 성격 |
|---------|------|------|
| `barca-data` | 수집·파싱·가공·모델 → `data/` | 네트워크·레이트 리밋·긴 실행 |
| `barca-dashboard` | `views/`, `_lib.py`, `app.py`, 사진 | 렌더 확인 필수 |
| `barca-qa` | 검수 전담 | 찾아서 보고만, 수정 금지 |

**두 곳에 있다.** 원본은 `barcelona/.claude/agents/`(레포와 함께 다님),
사본은 `D:\workspace\.claudegents\`(에이전트가 실제로 등록되는 것이
확인된 경로). 원본을 고친 뒤 반드시 다시 복사한다.

```bash
cp barcelona/.claude/agents/barca-*.md .claude/agents/
```

> 새로 만든 에이전트는 **그 세션에서는 안 잡힌다.** 목록을 시작할 때
> 한 번만 읽기 때문이다. 새 세션을 열어야 쓸 수 있다.

**셋의 담당 파일은 겹치지 않는다.** 최상위 스크립트 15개를 전부 배정해
미배정 0건·겹침 0건을 확인했다. `barca-data` 와 `barca-dashboard` 는
동시에 돌려도 안전하다.

경계에서 헷갈리기 쉬운 곳 둘:
- `assets/portraits/` 는 `crawl_portraits.py` 의 출력이라 **barca-data 몫**이다.
  `assets/` 의 나머지 사진 폴더는 barca-dashboard 몫이다.
- `train_model.py` 는 `views/model.py` 가 읽는 **산출물을 만드는 쪽**이라
  barca-data 몫이다.

### `tools/smoke.py`
페이지가 실제로 그려지는지 훑는다. `audit.py` 가 데이터를 본다면 이쪽은 화면이다.

**200 만 봐서는 안 된다** — Streamlit 은 페이지가 예외로 죽어도 200 을 준다.
그래서 `AppTest` 로 각 페이지를 **실행해** 예외를 잡고, HTTP 도 함께 본다.
페이지 목록은 `app.py` 의 `st.Page` 에서 읽으므로 페이지를 늘려도 따라간다.

```
"/d/workspace/EPL project/.venv/Scripts/python.exe" tools/smoke.py
```

일부러 예외를 넣어 잡히는지 확인했다(종료 코드 실패 1 / 정상 0).
다만 브라우저에서만 드러나는 것(CSS 무시, 라벨 겹침, 이미지 미로딩)은
여전히 사람이 봐야 한다.

### `.claude/agents/barca-qa.md`
검수 전담 서브에이전트. 데이터 무결성 → 페이지 렌더 → **화면 문구와 실제
수치의 일치** 세 층으로 본다. 이 프로젝트에서 실제로 터졌던 함정 일곱 가지를
표로 적어 뒀다(Streamlit 의 object-fit 덮어쓰기, FBref 열 이름 변동,
Squad Total 혼입, 대회 목록 하드코딩, StatsBomb 전체 이름, 캐시 키, 사진 캡션).

찾아서 보고만 하고 고치지는 않는다. 소스 수정·데이터 재생성·git 은 금지다.

## 2026-09-04 후속 검수 및 수정

- `2004/05` 원본의 `on_bad_lines="skip"` 때문에 버려지던 11경기를 복구했다.
  전체 1,265경기, 2004/05 38경기로 재생성했고 `tools/audit.py`에서 32/32 통과했다.
- 진행 중인 2026/27만 역대 우승 집계에서 제외하도록 `ongoing_season()`,
  `finished_seasons()`, `title_count()`를 `_lib.py`에 통합했다. 홈과 사이드바가
  같은 16회를 표시한다.
- 홈 득점·도움 리더의 표시명 매칭, StatsBomb 공개 범위의 데이터 기반 문구,
  패스 맵의 긴 선수명(`Xavier Hernández Creus` → `Xavi`)을 수정했다.
- `tools/smoke.py --no-http`: 17개 페이지 예외 없음. 브라우저 정상 폭(1440px)에서
  슛맵 피치 축 범위도 의도한 값으로 확인했다.

## 남은 작업

### 2026-09-10 라리가 결과 자동 갱신

- `.github/workflows/update-results.yml`이 매일 UTC 00:00(KST 09:00)에 실행된다.
- football-data.co.uk가 GitHub에서도 503을 반환해 OpenFootball의 공개 2026/27 일정·결과를
  기본 원본으로 바꿨다. 기존 SP1 스키마와 팀 이름으로 변환해 완료 경기만 덧붙인다.
  필수 열·바르사 경기·기존 경기 보존을 확인한 뒤에만 원본을 원자적으로 교체한다.
- 원본 파일이 바뀐 경우에만 `build_data.py`와 `tools/audit.py`를 거쳐 결과 파일을 커밋한다.
  변경이 없으면 재집계와 커밋을 생략한다. Cloud는 master 커밋을 자동 배포한다.
- GitHub 예약 작업은 정확한 시각 보장이 아니어서 KST 09:00 이후 다소 늦게 시작할 수 있다.
- 범위는 진행 중 시즌 라리가 최종 결과다. 경기 중 실시간 스코어·컵대회·선수 이벤트는 갱신하지 않는다.
- 첫 원본 실행 `34421590402`는 HTTP 503으로 실패했다. OpenFootball 전환 후 로컬에서
  30→41 리그 경기, 바르사는 3→4경기로 갱신했고 Valencia 원정 5-0을 반영했다.
- `tools/update_leaders.py`는 FotMob의 바르셀로나 라리가 통계에서 득점·도움 상위 5명을
  `current_leaders.json`에 저장한다. 홈은 최신 시즌이 일치하면 이 파일을 우선 사용한다.
- 자동 작업은 결과가 바뀌면 감독 집계도 함께 재생성한다. 수동 실행 `34423215583`이 전체 검증을 통과했다.
- Streamlit Cloud 홈에서 2026/27 4경기·17득점·2실점, 최근 Valencia 원정 5-0,
  득점 1위 Raphinha 6골과 도움 1위 Anthony Gordon 4도움까지 실제 반영을 확인했다.

### 2026-09-10 무료 2026/27 전체 페이지 스냅샷

- `tools/update_current_snapshot.py`가 FotMob의 무료 팀 엔드포인트에서 현재 시즌 선수·팀
  집계, 활성 명단, 완료 경기 결과를 `data/processed/current_2627.json`으로 저장한다.
  자동 갱신 workflow도 결과·홈 리더와 함께 이 파일을 매일 갱신한다.
- 대회별 성적, 선수 아카이브, 선수 고급기록, 티키타카 지수, 연계 네트워크, 슛 이벤트,
  패스 이벤트 페이지는 상단에 2026/27 일일 갱신 요약을 별도 표시한다.
- 무료 원천의 경계: 선수/팀 집계와 경기 결과는 최신화하지만, 개별 슛·패스 좌표와 패스-득점
  연결선은 받지 못한다. 따라서 현재 시즌 지도/그래프를 만들어 보이는 식으로 확장하지 말고,
  공개 StatsBomb 이벤트가 있는 과거 시즌 전용이라는 문구를 유지한다. 현재 시즌 좌표가 필요하면
  유료 이벤트 공급원 계약이 필요하다.

### 2026-09-10 레전드 선택 UI 복원

- 모바일 길이 단축을 위해 임시로 바꿨던 레전드 TOP10 선택 박스를 제거하고, 요청에 따라
  얼굴 이미지 카드와 `자세히` 버튼으로 고르는 기존 방식을 복원했다.

### 2026-09-10 2026/27 이적생 사진

- Transfermarkt 2026/27 스쿼드를 다시 수집해 현재 명단 27명의 사진·72px 썸네일을 모두
  갖췄다. 신규 7명(Dominik Livakovic, Brian Fariñas, Anthony Gordon, Jesse Bisiwu,
  Karim Adeyemi, Gabriel Jesus, Hamza Abdelkarim)의 사진을 추가했다.
- `crawl_portraits.py`가 앞으로 새 원본 사진을 받으면 썸네일까지 함께 생성하도록 보강했다.
- 사진 사용 근거의 최종 확인은 사용자 담당이며, 에이전트가 허가 완료로 표시하지 않는다.

### 2026-09-10 선택 UI 복원

- 레전드 TOP10에 이어 역대 감독도 이미지 카드와 `자세히` 버튼으로 고르는 기존 방식을 복원했다.
- 시대 분석의 티토·마르티노 카드에는 얼굴이 잘리지 않도록 전용 세로 위치를 적용했다.
- 역대 감독 카드와 상세 제목에 공식 감독 연표 기준 순번(제 n대)을 표시했다. Jesús Antonio de la Cruz의
  현재 저장소 행은 `file`이 비어 있어 이미지가 없는 상태다. 이미지를 추가하려면 해당 파일을
  `assets/managers/`에 넣고 `build_managers.py`의 `file`을 연결해야 한다.

### 2026-09-09 출시 전 후속 수정

- advanced의 임의 출전시간 보정을 폐기하고 이벤트 기록 경기당으로 변경했다.
  Messi 519경기·28,114패스 → 경기당54.17. 원본 캐시에는 교체 이벤트가 있으나
  배포용 집계에는 없어 실제 분 단위 재구성은 별도 작업이다. 무이벤트 출전은 분모에서 빠진다.
- coverage 사진을 시즌 비귀속으로 정정, passes의 0길이38건을 포함해367,738건 합계 일치.
- tikitaka 시대 라벨을 캐시 밖에서 적용. network는 누적 FBref 주 포지션의 거친 분류이며
  실제 경기 위치가 아님을 명시하고 미분류를 별도 배치. model은 단일 홀드아웃 결과로 한정.
- audit48/cache3/smoke17 및 변경별 회귀검사 통과. 코드 `fb683da`를 push했고,
  Cloud의 advanced/network/coverage/passes에서 새 문구와 집계를 확인했다.
- 사진 유지 및 사용 근거 사용자 확인 결정. 남은 검수는 docs/RELEASE_CHECKLIST.md 참조.
- 토큰 절감률 미측정. Sol 독립 작업과 요약 전달을 사용하며 동일 검사 반복을 제한한다.

### 2026-09-09 2012/13 MSN 시대 오표기 수정

- 티키타카 지수의 정점 2012/13 · 지수 95는 실제 계산값이지만, 뒤의 MSN은
  2012/13~2016/17을 통째로 MSN으로 묶은 편집 라벨 오류였다.
- 2012/13~2013/14는 티토·마르티노, 수아레스가 합류한 2014/15~2016/17만 MSN으로
  나눴다. 시대 분석·티키타카·연계 네트워크의 분류를 모두 같이 바꿨다.

### 2026-09-09 전체 검수 수정 기록 (후속 검수에서 완료 판정 정정)

- 당시 14건 모두 수정했다고 기록했으나 후속 코드 검수에서 출전시간 분모의 타당성,
  사진의 시즌 커버리지, 0길이 패스 누락이 미해결임을 확인했다. 최신 상태는
  `docs/RELEASE_CHECKLIST.md`와 `docs/CURRENT.md`를 따른다.
- 고급 기록은 라리가만 쓰고, 경기별 선수 시간 합이 11명×경기 길이가 되도록
  출전 추정치를 보정했다. 시즌별 최소 시간 범위도 실제 출전 추정치에 맞춰 자동 조정한다.
- 패스의 박스 투입은 박스 밖에서 안으로 이동한 17,611건만 센다. 슛의 제어는
  `슈팅 맵에서 골만 보기`로 범위를 명시했으며 요약·거리·신체 부위 차트에는 적용하지 않는다.
  선수 추이 차트는 상단 필터를 따른다.
- 레전드는 `통산`대신 `FBref 수록 기록`으로 표시하고, 대회별 `90분수`의 단위를
  바로잡았다. 점유율은 대회별 실제 표본 경기 수를 함께 보여 준다.
- 데이터 범위 지도는 StatsBomb의 1970~80년대 4시즌도 포함하고,
  football-data 1,265건은 `라리가 전체`가 아닌 `바르사 라리가 경기`로 표시한다.
- 라 마시아 현역 대표에서 Pedri를 빼고 Alejandro Balde를 넣었다. 홈 경기가 없는
  감독은 `nan%`대신 `기록 없음`을 보이고, 차비·플릭 설명의 경기당 승점은 실제 집계에서 읽는다.
- `AGENTS.md`에 이 프로젝트의 검수는 발견→수정→검증→문서→푸시→실배포 확인까지라는 규칙을 추가했다.
- 통합 검증: 문법, cache regression 3건, audit 48건, smoke 17페이지,
  diff 공백 검사 모두 통과.

### 2026-09-09 17페이지 전체 의미·데이터 검수

- Sol 3명이 17페이지를 세 묶음으로 나눠 수치·문구·필터·이름을 원본과 대조했다.
  audit 48건과 smoke 17페이지는 통과했지만 자동 검사가 잡지 못한 의미 결함 14건이 확정됐다.
- **최우선** `advanced.py`: 경기별 마지막 이벤트 분을 선수 출전 시간으로 합산해 531경기 중
  526경기가 11명×95분을 초과한다. per90·최소 분·순위가 오염됐다. 또 라리가라는
  문구와 달리 코파·UCL 77행이 섞여 있고, 200분 필터로 항상 비는 시즌 4개가 선택지에 있다.
- `passes.py`: `박스 투입`에 박스 안에서 시작한 5,767패스를 포함해 17,611건을
  23,378건으로 32.7% 부풀렸다.
- `shots.py`: `골만 보기`는 피치에만 적용되고 요약·거리·신체부위 차트는 전체 슛을 계속 집계한다.
- `players.py`: 선수 추이 선택기와 차트가 시즌·포지션·최소 경기·이름 필터를 무시한다.
- `legends.py`: FBref 부분 범위를 `통산 기록`으로 표시해 차비 672/767경기 등 실제 연혁과 모순된다.
- `competitions.py`: `90분수`를 `출전분`으로 잘못 표시한다. 평균 점유율도 라리가 456/1,262경기,
  UCL 120/325경기만 있는 부분 표본인데 전체 범위처럼 보인다.
- `coverage.py`: 커버리지 지도가 1993/94부터만 그려 StatsBomb의 1970~80년대 4시즌을 빼먹는다.
  또 `club_matches` 1,265건을 `라리가 전 경기`라고 설명하지만 실제로는 바르사 경기만이다.
- `masia.py`: Pedri를 `지금의 세대` 라 마시아 계보에 넣었지만 페이지 자체 판정 데이터는
  Pedri를 유스 출신으로 분류하지 않는다.
- `managers.py`: Jesús Antonio de la Cruz는 홈 0경기라 `nan%`가 표시되며,
  차비·플릭의 설명 중 경기당 승점이 현재 집계 2.25·2.42와 다르다.
- 확인된 결함이 없던 페이지: 홈, 시대, 엘클라시코, 티키타카, 연계 네트워크,
  삼각편대, 시즌 검색, AI 모델, 앱 내비게이션.
- 다음 수정 순서: advanced 분모·범위 → passes/shots/players 필터 →
  legends/competitions/coverage 표기 → masia/managers 소규모 수정.

### 2026-09-09 연계 네트워크 호나우지뉴 오매칭 수정

- 2005/06~2007/08의 `Ronaldo`는 호나우두가 아니라 StatsBomb 법적 이름
  `Ronaldo de Assis Moreira`, 즉 호나우지뉴였다. Understat은 2006/07 데이터가
  없으므로 이 구간의 원인이 아니다.
- StatsBomb 슛과 key-pass 연결은 정상이고, 전 시즌 FBref 명단을 대상으로 한
  토큰 매칭이 1996/97의 `Ronaldo`를 골라 두 사람을 합친 것이 원인이다.
- `_lib.py` `SB_ALIAS`에 `Ronaldo de Assis Moreira → Ronaldinho`를 추가했다.
- 2006/07은 호나우지뉴 5득점·7도움으로 복구됐다. `tools/audit.py`에
  2004년 이후 `Ronaldo` 오매칭 0건과 2006/07 5득점·7도움 회귀 검사를 추가했다.
- 전체 21시즌·1,264개 도움 포함 골을 명단과 대조했으며, 다른 명백한 선수
  오인은 없었다. 나머지 23개는 `Anssumane Fati/Ansu Fati` 등 동일 선수의 표기 차이다.
- 검증: audit 48건, smoke 17페이지, diff 공백 검사 통과.

### 2026-09-09 로드맵 3 — 17페이지 반응형·시각 검수

- Sol 에이전트로 17개 라우트를 데스크톱 1440×900, 모바일 390×844에서 확인했다.
  페이지 예외, 문서 전체 가로 넘침, 깨진 이미지는 재현되지 않았다.
- 모바일에서 10명 레전드·19개 감독 카드가 모두 세로로 쌓이던 선택 구간을
  이름·기간이 들어간 컴팩트 선택기로 바꾸었다. 선택된 인물의 큰 사진과 상세는 유지한다.
- 연계 네트워크는 최신 시즌을 기본으로, 최소 연결 2회로 시작한다. 전체 시즌
  차트는 760px로 높이를 제한하며 `전체`를 직접 고르면 볼 수 있다.
- 남은 제약: 라 마시아·데이터 범위 표는 모바일에서 내부 가로 스크롤을 쓴다.
  Streamlit Cloud 소유자용 `Manage app` 배지는 앱 코드 요소가 아니다. 사이드바의 7개
  메뉴는 Streamlit 기본 `View 7 more`에 들어간다.
- 검증: 문법 검사, cache regression 3건, audit 46건, smoke 17페이지,
  diff 공백 검사 모두 통과.
- 다음은 로드맵 4: 주요 필터·차트의 상호작용과 키보드·스크린리더 접근성 정밀 검수.

### 2026-09-09 로드맵 2 — 데이터 갱신·캐시 안정화

- Sol 에이전트 세 명을 최소 문맥으로 실행해 캐시 감사, coverage UI, 회귀검사를 분담했다.
- coverage와 시즌 검색의 인자 없는 파일 캐시를 제거하고 공통 로더를 사용한다.
- 선수 사진 썸네일, FBref 이름 원본, 감독 전술의 StatsBomb 경기, 네트워크와 MVP
  이름 매칭 등 캐시가 간접 의존하던 파일·폴더도 키에 포함했다.
- 데이터 제공 범위 표에 실제 시즌 범위, 최신 관측값, 파일 갱신 시각(KST)을 표시한다.
- `tools/test_cache_invalidation.py`에서 Parquet·JSON·디렉터리 캐시 갱신 3건 통과.
  `tools/audit.py` 46건과 `tools/smoke.py --no-http` 17페이지도 모두 통과했다.
- 다음 단계는 로드맵 3: 정상 데스크톱 폭과 모바일 폭에서 17페이지 시각 검수.

### Headroom·Task Observer 설치 검증

- 두 도구 로컬 설치 완료. 상세 경로·버전·재현 방법은 docs/TOKEN_BENCHMARK.md.
- Headroom 실제 인계/소스 0%, 합성 반복 로그 98.7% 절감. 오류 sentinel 보존.
- 데스크톱 채팅 프록시 연결/계정 한도 절감/Observer 자동 활성화는 미검증.
- 앱 소스 변경 없음. 도구 검증 완료 후 로드맵 2로 복귀한다.

### 토큰 절약 운영 반영

- 사용자 제공 글의 Headroom·Task Observer 저장소를 확인했다. 외부 도구 설치나
  프록시 연결은 적용하지 않았다. 이 데스크톱 대화에 대한 실제 절감률은 미측정이다.
- AGENTS.md에 최소 문맥 에이전트, 필요한 절만 읽기, 중복 검사·출력 최소화를 기록했다.
- docs/CURRENT.md를 짧은 재개 지점으로 추가했다. 다음은 로드맵 2 캐시 안정화다.

### 2026-09-09 로드맵 1 — 숫자·설명 정합성

- 세 에이전트를 데이터 검증, 화면 수정, 독립 QA로 분리했다. 재사용 프롬프트는
  `docs/AGENT_PROMPTS.md`에 있으며 메인이 문서·통합·배포를 맡는다.
- 직접 재계산: 전체 34시즌 1,265경기, 823승 240무 202패, 2,889득점 1,228실점.
  완료 33시즌은 1,262경기, 820승 240무 202패, 2,877득점 1,226실점, 우승 16회.
- 엘클라시코는 66경기, 31승 14무 21패, 124득점 90실점. README 수치와 페이지
  목록을 갱신하고 coverage·시즌·클라시코의 2004/05 복구 전 설명을 정리한다.
- 감독 집계에서 미배정 1경기와 중복 1경기가 상쇄돼 총경기만 맞았던 오류를 발견했다.
  2003-02-01은 Jesús Antonio de la Cruz, 2021-11-06은 Sergi Barjuan의 경기다.
  귀속 보정과 진행 시즌 우승 제외를 적용하며 상세 화면도 같은 경기 선택을 사용한다.
- 감독 산출물 재생성 후 전체 전적·득실·우승 합계 일치, 경기별 정확히 1회 배정을 확인했다.
  원본 CSV 독립 대조를 포함한 audit 46개 검사 모두 통과했다.
- 시대 분석은 완료 시즌 기준으로 통일하고 경기당 승점은 총승점/총경기로 계산한다.
- StatsBomb 범위는 관련 화면에서 실제 데이터로 표시하고 희소 표본 한계를 설명했다.
- 최종 로컬 검증: audit 46개 통과, smoke 17페이지 예외 없음, diff 공백 검사 통과.
  코드 커밋 `5eea932`를 GitHub에 push했다. 공개 /network 화면에서 이번에 수정한
  시작 시즌 2005/06 문구와 정상 차트·1,264건 표시를 확인해 새 코드 반영을 확인했다.
  전체 17페이지의 브라우저 시각 검수는 로드맵 3에서 수행한다.
- 다음 단계: 데이터 파일 갱신 시 캐시 반영과 갱신일·범위 표시 안정화.

### 2026-09-09 일일 개발 루틴 설정

- Codex 이 채팅방의 예약 작업 `09-15-kst`를 활성화했다. 매일 KST 09:15 실행.
- 데이터 검증·UI 검수 에이전트를 필요에 따라 생성하며 메인이 통합·문서·배포를 담당한다.
- `AGENTS.md`에 지속 운영 기준을 기록했다. 매회 작은 개선 1개를 검증하고 이 문서에 결과와 다음 시작점을 남긴다.
- 검증된 변경만 GitHub master에 push하고 Streamlit Cloud의 실제 변경 화면을 확인한다.
- HTTP 200만 확인한 이전 보고는 새 커밋 배포를 입증한 것이 아니다. 배포 확인 시 이를 구분한다.
- 다음 실행은 최신 작업 상태부터 확인하고 데이터 정확성·UI·성능 중 근거 있는 개선을 선정한다.
- 초기 검수 에이전트 발견: README의 페이지 수·전적 합계와 현재 데이터가 불일치하며, 이 문서 및 views/coverage.py에 2004/05 복구 전 누락 설명이 남아 있다. 첫 개선에서 실제 산출물과 대조해 정리한다.
- 후속 후보: views/coverage.py의 source_coverage() 캐시에 데이터 파일 수정 시각이 반영되는지 확인하고 갱신을 보강한다.

기존 개별 요청은 처리됐으며, 이후 개선은 위 일일 루틴에서 선정하고 기록한다.

가비·페르민 사진은 Getty Images 보도사진이라 워터마크가 있다. 교체 대신
**출처 표기**로 처리하기로 했다(사용자 결정). `assets/masia/credits.json` 에
실제 출처를 적었고, 라 마시아 페이지 '지금의 세대' 아래에 캡션으로 밝혔다.
나중에 자유 라이선스 사진을 구하면 `assets/_inbox/` 에 `gavi.jpg`,
`fermin.jpg` 로 넣고 `python apply_inbox.py` 를 돌리면 된다.
그때 credits.json 의 해당 항목도 함께 고쳐야 한다.

## 확인해 본 결과 손댈 필요 없던 것

### 시즌 간 표본 차이 — 문제 없음
"시즌마다 표본이 달라 분산도가 흔들리는 것 아니냐"를 검증했다. **아니다.**
- 골 수와 분산도의 상관은 **-0.11** 로 사실상 없다
- 가장 집중된 2015/16(79골·분산도 81.7)을 가장 분산된 2021/22와 같은
  표본 크기(51골)로 줄여 200번 다시 뽑아도 평균 **81.5**(95% 구간 79.4~83.4)
- 2021/22 의 92.8 은 그 구간 밖 → **두 시즌 차이는 실제 구조 차이다**

보정 기능을 만들지 않고 이 결과를 페이지 하단에 적었다. 단, **조합 수처럼
개수를 세는 값**은 여전히 시즌끼리 곧바로 견주면 안 된다(도움 기록률이
시즌마다 61~73%).

