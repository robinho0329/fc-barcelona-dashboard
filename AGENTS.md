# 대시보드 개발 에이전트 운영

도구를 사용하는 작업 세션에서는 첫 도구 호출 전에 task-observer 스킬을 불러
Session Start Protocol을 실행한다. 관찰 저장소는
`C:\Users\xcv54\.codex\skill-observations`로 고정한다. 작업 종료 보고에는
관찰 기록 여부를 확인한다. 스킬이 로드되지 않으면 이 규칙의 축약본으로 대신
진행하지 말고 해당 사실을 보고한다.

작업 시작 시 docs/CURRENT.md와 최신 Git 상태를 읽는다.
README.md와 HANDOFF.md는 필요한 절을 검색해 읽고, 매번 전체를 다시 읽지 않는다.
사용자는 독립 작업의 서브 에이전트 병렬 수행을 승인했다.

- 메인: 개선 우선순위, 구현 통합, 문서, 커밋 및 배포 확인을 담당한다.
- 데이터 검증 에이전트: 수치·집계·출처·캐시를 검토하고 근거를 보고한다.
- UI 검수 에이전트: 정상 화면 크기에서 문구·차트·이미지와 동작을 확인한다.
- 실제 독립 작업이 있을 때 필요한 에이전트만 생성하고 파일 소유 범위를 정한다.
- 에이전트는 기본 fork_turns="none"으로 생성한다. 저장소 경로, 목표, 담당 파일,
  완료 기준, 필요한 사실만 전달한다. 전체 대화 복제는 의존성이 있을 때만 사용한다.
- 독립 작업 2개면 에이전트 2개만 사용한다. 단순 수정에는 별도 에이전트가 필요 없다.
- 보고는 변경 파일·검증·문제·다음 행동 위주로 짧게 하고 원시 로그는 실패 때만 읽는다.
- 테스트는 필요한 것을 한 번 실행하고 새 변경이나 실패 없이 반복하지 않는다.
- 수치 계산은 스크립트로 정확히 수행한다. 통계 원본·실패 로그를 손실 압축하지 않는다.
- 사용자 수정 요청은 이 문서나 관련 지침에 짧게 남겨 같은 실수를 반복하지 않는다.
- 사용자가 전체 프로젝트나 여러 페이지의 `검수`를 요청하면, 확정된 결함은 보고로만
  끝내지 말고 안전한 범위에서 수정·회귀검사·문서·push·실배포 확인까지 한 작업으로
  완료한다. 수정 권한이 없거나 파괴적 변경이 필요할 때만 중단하고 사용자에게 묻는다.
- 모델은 사용자 설정을 유지한다. 비용 절감률은 실제 측정 없이 단정하지 않는다.

매일 KST 09:15 이 채팅방에 연결된 Codex 예약 작업이 작은 개선을 수행한다.
매번 HANDOFF.md에 날짜, 변경, 검증, 미완료 사항과 다음 시작점을 기록한다.
README.md는 현재 기능·데이터·운영 설명이 달라지면 함께 갱신한다.
관련 검증이 통과한 이번 변경만 커밋하고 origin/master로 일반 push한다.
타인의 변경을 덮어쓰거나 함께 커밋하지 않는다.
Streamlit Cloud는 GitHub 연동 배포를 사용한다. 변경 화면을 확인해야 새 배포
완료로 보고할 수 있으며 HTTP 200은 접근 가능 여부만 증명한다.
매회 결과는 원래 채팅방에 개선 내용, 검증, 커밋, 배포 상태, 다음 후보로 보고한다.
